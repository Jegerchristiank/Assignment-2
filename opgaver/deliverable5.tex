\subsection{Deliverable 5: Notebook Submission (\texttt{assignment.ipynb})}
\label{sec:deliverable5}

\subsubsection{Submission Note}
The completed notebook is submitted as a separate file and is not reproduced in full in this report. It contains the full Part II workflow (Parts 0-7), from initial exploration and method execution to custom DQ analysis and invisible-work assessment.

\subsubsection{Notebook Scope (Parts 0--7)}
The notebook covers the full analytical chain requested in the assignment: RQ framing, data exploration, method selection, execution of five selected methods (M2, M6, M8, M10, M11), custom DQ dimension implementation (\textit{Batch Logging Credibility}), per-facet synthesis, and invisible-work mapping against the workflow diagram.

\subsubsection{Repository Path and Execution Environment}
Notebook path:
\texttt{download fra github classroom/assignment-2-afleveringsopgaver-1-main/2025-2026/assignment2/code/assignment.ipynb}

Execution was performed in a local virtual environment in the classroom repository root (\texttt{.venv}) with Python 3.12.1 and dependencies from \texttt{requirements.txt} (pandas 2.2.2 and numpy 1.26.4). A sequential runtime test of notebook code cells was completed without execution errors.

\subsubsection{Completion Status}
Part II notebook tasks are complete. No unresolved \texttt{TODO} or \texttt{YOUR CODE HERE} placeholders remain. Selected method outputs are stored in reusable objects (\texttt{res\_m2}, \texttt{res\_m6}, \texttt{res\_m8}, \texttt{res\_m10}, \texttt{res\_m11}), and the custom method \texttt{my\_custom\_dq\_method(df)} is implemented and executed with the required output structure (\texttt{case\_flags}, \texttt{summary\_metrics}, \texttt{examples}). Part 7 includes a completed invisible-work table with at least three activities not explicitly logged.
