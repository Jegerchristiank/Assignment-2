\subsection{Deliverable 2: Event Log Representing the Observed Routine}
\label{sec:deliverable2}

\subsubsection{Observation Procedure and Documentation}
The observation was conducted on February 20, 2026 (06:45-07:30), two days after the interview on February 18, 2026. This satisfies the required delay between interview and observation.

A short observation guide was used to focus on sequence, tools, interactions, and deviations from the interview baseline. Notes were captured as descriptive notes (what happened, when, with which tool) and reflexive notes (questions/uncertainties). The event log was transcribed from descriptive notes only.

The full machine-readable event log is submitted as a separate CSV artifact with timestamps, event labels, and context notes.

\subsubsection{Event Log Schema}
The submitted CSV uses the following fields: \texttt{case\_id}, \texttt{event\_id}, \texttt{timestamp} (ISO datetime), \texttt{event\_label}, \texttt{actor}, \texttt{tool}, \texttt{context\_note}, and \texttt{related\_activity\_id} (A01-A09 or NA).

\subsubsection{Observed Event Sequence (Report Excerpt)}
The required observed event-sequence excerpt is provided in Appendix~\ref{app:part1_supporting}, Table~\ref{tab:d2_observed_excerpt}. In the main text, the key comparison anchors are E03 (extra coordination), E09 (order shift), and E10 (workaround).

\subsubsection{Observation Summary for Delta Analysis}
The observation confirms the same overall routine boundary as Deliverable 1, but includes practical variation and coordination not present in the interview baseline. In particular, one extra coordination event (E03), one order change (E09 relative to A04/A05), and one explicit workaround (E10) are central for the delta sheet in Deliverable 3.
