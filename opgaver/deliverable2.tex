\subsection{Deliverable 2: Event Log Representing the Observed Routine}
\label{sec:deliverable2}

The observation was conducted on February 20, 2026 (06:45--07:30), two days after the interview. This delay was intentional to reduce recall carry-over between elicitation and observation.

In methodological terms, the observation captures \textit{work-as-done} (what was actually performed in context). The resulting CSV captures \textit{work-as-recorded} (what is represented in structured trace data) \parenciteshort{guidehealthinformatics2026ch27,zajac2026methodsknowing}. To keep data collection transparent, the observation followed a short guide (Appendix~\ref{app:part1_supporting}, Table~\ref{tab:d2_observation_guide}).

The submitted event log includes the fields \texttt{case\_id}, \texttt{event\_id}, \texttt{timestamp}, \texttt{event\_label}, \texttt{actor}, \texttt{tool}, \texttt{context\_note}, and \texttt{related\_activity\_id}. The full machine-readable file is submitted separately, while Appendix~\ref{app:part1_supporting}, Table~\ref{tab:d2_observed_excerpt} shows the key excerpt used in the report.

Compared with the interview baseline, the observation reveals three anchor differences that are used in Deliverable 3:
\begin{itemize}
  \item E03: extra coordination event,
  \item E09: order shift relative to the interview sequence,
  \item E10: explicit workaround behaviour.
\end{itemize}

These observations provide the empirical basis for the delta analysis between stated and observed practice.
